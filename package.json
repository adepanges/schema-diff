{
  "name": "schema-diff",
  "version": "1.0.0",
  "description": "Migration-tool agnostic database schema diff — GitHub Action and CLI",
  "main": "dist/main.js",
  "bin": {
    "schema-diff": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "pretest": "tsc --noEmit",
    "test": "jest --coverage",
    "test:unit": "jest tests/unit"
  },
  "keywords": [
    "schema",
    "diff",
    "migration",
    "database",
    "github-action",
    "dbml"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=24.13.0"
  },
  "packageManager": "pnpm@10.30.3",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "@dbml/core": "^6.4.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.3.5",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
